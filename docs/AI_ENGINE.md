# AI Engine

## Overview

The AI Engine is one of the core components of the AI-Powered Intelligent Sandbox. It enhances traditional cybersecurity analysis by transforming technical findings into human-readable explanations, risk assessments, and actionable recommendations.

Unlike cloud-based AI services, this project uses **Ollama** to run **Large Language Models (LLMs)** locally, ensuring complete privacy, offline operation, and full control over the AI environment.

The AI Engine **does not replace traditional security analysis**. Instead, it acts as an intelligent assistant that interprets the results generated by the Security Analysis Engine.

---

# Objectives

The AI Engine has the following objectives:

- Explain complex security findings in simple language
- Assist users in understanding suspicious files
- Generate investigation summaries
- Recommend next investigation steps
- Support cybersecurity learning
- Reduce manual report writing
- Improve investigation efficiency

---

# Why Ollama?

Ollama was selected because it provides a lightweight and secure way to run modern open-source Large Language Models locally.

## Advantages

- Completely offline
- No API costs
- No internet dependency
- No rate limits
- Better privacy
- Faster local inference
- Easy model switching
- Supports multiple open-source models

---

# Why Local AI Instead of Cloud AI?

Many AI services require uploaded data to be sent to external servers.

For cybersecurity investigations, uploaded files may contain:

- Confidential documents
- Internal company files
- Malware samples
- Sensitive information

Running AI locally ensures:

- Complete privacy
- Better security
- No external data transmission
- Compliance with organizational policies

---

# AI Architecture

```
               Static Analysis

                      │

                      ▼

              Analysis Results

                      │

                      ▼

             AI Prompt Builder

                      │

                      ▼

               Ollama Server

                      │

                      ▼

             Open Source LLM

                      │

                      ▼

           AI Generated Response

                      │

                      ▼

         Dashboard & PDF Report
```

---

# AI Workflow

The AI Engine does not directly analyze uploaded files.

Instead, it receives structured analysis data generated by the Security Analysis Engine.

Example workflow:

```
Upload File

↓

Static Analysis

↓

Metadata

↓

SHA-256

↓

YARA Matches

↓

Threat Score

↓

Prompt Builder

↓

Ollama

↓

AI Explanation

↓

Store Results

↓

Generate Report
```

---

# Supported Models

The architecture is model-independent.

Any Ollama-compatible model can be used.

Recommended models include:

| Model | Purpose |
|---------|----------|
| Qwen 3 | General reasoning |
| DeepSeek-R1 | Security reasoning |
| Llama 3 | Balanced performance |
| Mistral | Lightweight deployment |
| CodeLlama | Code analysis |
| Qwen2.5-Coder | Source code understanding |

Future models can be integrated without changing the application architecture.

---

# AI Responsibilities

The AI Engine performs the following tasks:

- Threat explanation
- Executive summary generation
- Technical summary generation
- Investigation recommendations
- IOC interpretation
- MITRE ATT&CK explanation
- Report enhancement
- Security education

---

# AI Input

The AI receives structured JSON instead of raw files.

Example

```json
{
    "filename":"sample.exe",
    "sha256":"abc123...",
    "file_type":"PE32 Executable",
    "entropy":7.9,
    "yara_matches":["Emotet"],
    "imports":[
        "VirtualAlloc",
        "CreateRemoteThread"
    ],
    "threat_score":84
}
```

---

# AI Output

Example response

```json
{
    "summary":"The uploaded executable exhibits characteristics commonly associated with malware.",
    "risk":"High",
    "recommendation":"Perform additional investigation before executing this file."
}
```

---

# Prompt Engineering

Prompt engineering plays a critical role in obtaining reliable responses.

Instead of sending vague prompts, the AI receives structured context.

Example

```
You are an experienced Cybersecurity Analyst.

Analyze the provided investigation results.

Explain:

• Why the file is suspicious.

• Important findings.

• Possible attacker behavior.

• Recommended analyst actions.

Keep the explanation technical but understandable.
```

---

# Explainable AI (XAI)

The platform focuses on Explainable AI.

Instead of simply saying

```
Malicious
```

the AI explains:

- Why it reached that conclusion
- Which indicators influenced the decision
- Confidence level
- Supporting evidence
- Suggested next steps

---

# AI Analysis Pipeline

```
Security Analysis

↓

Metadata

↓

Hashes

↓

YARA Results

↓

Detection & Evidence Engine (Phase 3)
- normalized evidence
- IOC extraction
- correlated findings
- evidence-backed MITRE mappings
- provenance graph

↓

Threat Score

↓

Prompt Builder

↓

Ollama

↓

LLM Response

↓

Validation

↓

Database

↓

Dashboard
```

---

## Phase 3 boundary — the AI explains, it does not invent

Phase 3 delivers a fully deterministic Detection & Evidence Engine (evidence
normalization, IOC extraction, correlation rules, evidence-backed MITRE
mappings, provenance graph). Every verdict, score and technique mapping can be
traced back to concrete evidence without any model in the loop.

Phase 4 (implemented) **explains** results the deterministic engine already
produced. The AI must not invent findings, IOCs or MITRE techniques that the
engine did not observe — it interprets and summarizes, it never creates
detections.

---

# AI Engine Implementation (Phase 4)

## Provider abstraction (`app/services/ai/providers.py`)

- `AIProvider` contract: `label()`, `list_models()`, `select_model()`,
  `generate(prompt, model)`.
- `OllamaProvider`: talks only to a locally hosted server (`OLLAMA_URL`,
  default `http://localhost:11434`). **No cloud/paid inference exists anywhere.**
- Every network call is bounded by a timeout and maps to `AIUnavailable` on any
  HTTP/transport error — a missing or broken Ollama never blocks analysis.

## Model selection (config-driven, never hard-coded)

1. `OLLAMA_MODEL` (`Settings.ai_model`) when set, else
2. the first installed model reported by `GET /api/tags` (auto-discovery), else
3. `AIUnavailable` → the `/ai` endpoint reports `unavailable`.

Only locally installed (free) models are ever used.

## Prompt builder (`app/services/ai/prompt.py`)

Builds one deterministic prompt from the Phase 3 output: file identity,
classification, the authoritative threat score + breakdown, findings with
confidence/module/MITRE, IOCs, the deterministic MITRE mappings, and an
evidence inventory. The prompt hard-constrains the model: never invent IOCs or
technique ids, never assign a family or score, never claim runtime behaviour,
answer with JSON only.

## Strict validation (`app/services/ai/validation.py`)

- Parses the JSON response (tolerates ```json fences and trailing commas).
- Coerces types, bounds every list/text/confidence value.
- **Anti-hallucination guard:** every `mitre_explanation` entry must reference a
  technique id that exists in the deterministic mappings — anything else is
  dropped. The model can never introduce IOCs or a score: those fields do not
  exist in the accepted schema.

## Service (`app/services/ai/service.py`)

`run_ai_analysis(context)` returns one of three stable shapes:

| Status | Meaning |
|--------|---------|
| `completed` | validated interpretation + deterministic context + provenance (cached by the router) |
| `unavailable` | Ollama unreachable / no model / model missing — deterministic analysis unaffected |
| `error` | model output failed strict validation and was rejected (never stored/shown) |

Completed AI results are cached on the stored analysis payload; unavailable and
error states are **not** cached, so a later-installed Ollama is picked up
automatically.

## Response fields

`executive_summary`, `technical_summary`, `threat_explanation`, `key_findings`,
`risk_factors`, `mitre_explanation` (validated ids only), `recommendations`
(priority + action), `confidence` (0–100, clamped), `business_impact`,
`limitations`, plus deterministic context (`severity`, `verdict`,
`score_total`, `family`, `classification`) and `provenance` counts.

---

# AI Features

## Executive Summary

A short explanation suitable for managers or non-technical users.

---

## Technical Summary

Detailed explanation intended for cybersecurity analysts.

---

## Threat Explanation

Explains:

- Suspicious indicators
- Malware characteristics
- Possible attack methods

---

## Recommendations

Suggests:

- Additional investigation
- Quarantine
- IOC blocking
- Threat intelligence lookup

---

## Risk Assessment

Provides an AI-assisted interpretation of the calculated threat score.

---

# AI Limitations

The AI Engine does **not**:

- Execute malware
- Guarantee malware detection
- Replace antivirus software
- Replace professional analysts

It serves as an intelligent assistant that complements traditional analysis techniques.

---

# Privacy Considerations

One of the major advantages of using Ollama is privacy.

The AI Engine:

- Never uploads files to external servers
- Operates completely offline
- Keeps investigation data local
- Allows deployment in isolated environments

---

# Security Considerations

To prevent unsafe AI behavior:

- AI receives structured analysis instead of raw executable code
- User prompts are controlled by the application
- Responses are validated before display
- Investigation results remain immutable
- No external API communication is required

---

# Performance Optimization

Future optimization strategies include:

- Model caching
- Prompt templates
- Streaming responses
- GPU acceleration
- Batch AI requests
- Quantized models for lower memory usage

---

# Future AI Enhancements

## AI Chat Assistant

Users will be able to ask questions such as:

- Why is this file suspicious?
- Explain the YARA match.
- What does this API import indicate?
- Summarize this investigation.
- What should I investigate next?

---

## AI Report Writer

Automatically generates:

- Executive reports
- Technical reports
- Incident summaries
- Investigation conclusions

---

## AI IOC Analysis

Explain relationships between:

- IP addresses
- Domains
- URLs
- Hashes

---

## AI MITRE Mapping

Automatically map investigation findings to:

- ATT&CK Tactics
- ATT&CK Techniques
- ATT&CK Procedures

---

## AI Rule Recommendation

Recommend:

- YARA rules
- Sigma rules
- Detection improvements

---

## AI Malware Classification

Predict likely malware family based on static indicators.

Examples:

- Ransomware
- Trojan
- Backdoor
- Downloader
- Infostealer

---

## AI Threat Hunting

Future AI capabilities include:

- Finding related investigations
- Detecting investigation patterns
- Suggesting similar malware samples
- Identifying recurring indicators

---

## Retrieval-Augmented Generation (RAG)

Future versions may include a local RAG pipeline.

Knowledge sources may include:

- Previous investigations
- Internal documentation
- MITRE ATT&CK
- YARA documentation
- Malware reports
- Threat intelligence feeds

This enables the AI to answer questions using trusted project-specific knowledge instead of relying solely on the language model.

---

## Multi-Model Support

The AI Engine will allow users to switch between installed Ollama models depending on their requirements.

Example:

| Model | Best For |
|---------|-----------|
| Qwen 3 | General analysis |
| DeepSeek-R1 | Security reasoning |
| Llama 3 | Balanced performance |
| Mistral | Fast inference |
| CodeLlama | Code analysis |

---

# AI Module Workflow

```
Analysis Completed

↓

Prepare JSON Context

↓

Generate Prompt

↓

Send to Ollama

↓

LLM Processing

↓

Receive Response

↓

Validate Output

↓

Store Database

↓

Generate Report

↓

Display Dashboard
```

---

# Current Status

| Feature | Status |
|---------|--------|
| AI Module Architecture | ✅ Completed |
| Detection & Evidence Engine (Phase 3) | ✅ Completed |
| Ollama Provider & Model Discovery | ✅ Completed (Phase 4) |
| Prompt Engineering | ✅ Completed (Phase 4) |
| Response Schema & Validation | ✅ Completed (Phase 4) |
| Executive Summary | ✅ Completed (Phase 4) |
| Technical Summary | ✅ Completed (Phase 4) |
| Threat Explanation | ✅ Completed (Phase 4) |
| Recommendation Engine | ✅ Completed (Phase 4) |
| Graceful Ollama-unavailable fallback | ✅ Completed (Phase 4) |
| AI Chat Assistant | 📅 Future |
| RAG Integration | 📅 Future |
| Multi-Model Support | 📅 Future |

---

# Summary

The AI Engine transforms technical cybersecurity findings into understandable, actionable intelligence by combining traditional static analysis with locally hosted Large Language Models through Ollama. By keeping all processing on the user's machine, the platform prioritizes privacy, security, and explainability while providing an extensible foundation for future AI-powered investigation capabilities such as conversational analysis, malware classification, Retrieval-Augmented Generation (RAG), and intelligent threat hunting.