# ─── Aegis Sandbox AI · Frontend Environment ───────────────────────────────
# The prototype runs entirely on realistic mock data by default.
# Set VITE_USE_BACKEND=true only when the FastAPI backend is running.

VITE_USE_BACKEND=false
VITE_API_BASE_URL=/api
VITE_APP_NAME="Aegis Sandbox AI"
