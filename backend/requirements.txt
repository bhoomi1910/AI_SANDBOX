# ─── AI-Powered Intelligent Sandbox · Backend ────────────────────────────────
# Core API
fastapi==0.140.13
uvicorn[standard]==0.51.0
pydantic==2.13.4
pydantic-settings==2.15.0
python-multipart==0.0.32

# Persistence (SQLite by default; PostgreSQL later via DATABASE_URL)
SQLAlchemy==2.0.51
psycopg2-binary==2.9.10  # PostgreSQL driver (unused while DATABASE_URL is SQLite)

# PDF report generation (Phase 7)
reportlab==5.0.0

# Utilities
python-dotenv==1.2.2
httpx==0.28.1

# Tests
pytest==9.1.1
pypdf==6.15.0  # PDF text extraction used in report tests

# ─── Later phases (uncomment when implemented) ───────────────────────────────
# Static analysis (Phase 2):
# yara-python==4.5.1
# oletools==0.60.2
# python-magic==0.4.27
# ssdeep==3.4
# pefile==2023.2.7
#
# AI engine (Phase 4) — Ollama client, no API keys required:
# httpx already covers the Ollama REST API.
