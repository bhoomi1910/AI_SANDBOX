# ─── Aegis Sandbox AI · Backend ─────────────────────────────────────────────
# Core API
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
pydantic-settings==2.3.4
python-multipart==0.0.9

# Persistence
SQLAlchemy==2.0.31
# psycopg2-binary==2.9.9      # uncomment for PostgreSQL (SQLite works out of the box)

# Task queue / cache (optional — used in production topology)
redis==5.0.7
celery==5.4.0

# AI / RAG stack (optional — the prototype ships a deterministic simulator so
# it runs with zero API keys; install these to enable the real LLM pipeline)
# langchain==0.2.6
# langchain-community==0.2.6
# faiss-cpu==1.8.0
# openai==1.35.7
# anthropic==0.30.1

# Utilities
python-dotenv==1.0.1
httpx==0.27.0
